/*
 * The `nav-menu` block only carries the authored navigation rows; the header
 * block reads them and builds the mega menu. Nothing to decorate here — the
 * block exists so the rows stay untouched inside the /nav fragment and so the
 * Universal Editor has a component to bind the navigation model to.
 */

// eslint-disable-next-line no-unused-vars
export default function decorate(block) {
  // intentionally empty — see blocks/header/header-menu.js
}
